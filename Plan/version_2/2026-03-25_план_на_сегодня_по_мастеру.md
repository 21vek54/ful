## План на сегодня: master

Дата: 2026-03-25

### Актуализация на текущий момент (вторая половина дня)

- Этапы `1-5` по `master` считаем закрытыми в рамках задачи на сегодня.
- Минимальный `master` фиксируем как транспортную основу:
  - self-init
  - `Wi-Fi`
  - `MQTT`
  - `I2C`-опрос плат `12` и `13`
  - публикация `online / offline` в `MQTT`
- Следующий фокус переносим на уровень `MQTT` / `Node-RED` на рабочем ПК, а не на дальнейшее усложнение прошивки.

### Зафиксированные уточнения

- Сначала понять, что есть в `master`, и только потом чистить.
- Новую структуру делать обязательно с учетом правил и ограничений `PlatformIO`.

### Цель на сегодня

Подготовить `master` к нормальной пересборке: сначала получить понятную картину текущего состояния, затем определить, что оставить, что удалить, и на этой базе наметить новую минимальную структуру проекта и верхний слой `MQTT` / `Node-RED`.

### Приоритет дня

1. Не начинать с удаления файлов и кода.
2. Сначала сделать ревизию текущего `master`.
3. После ревизии принять решение по очистке.
4. Только потом переходить к новой иерархии и верхнему слою.

### План на сегодня

#### Этап 1. Разобрать текущий `master`

- Просмотреть состав проекта `master_control_com12`.
- Составить список файлов, модулей и крупных блоков логики.
- Отдельно отметить:
  - сетевой слой
  - MQTT-часть
  - бизнес-логику
  - временный код
  - дубли
  - спорные куски

Результат этапа:
- понятный список того, что реально есть в `master`
- черновая карта: `что это`, `зачем нужно`, `кандидат оставить / удалить / под вопросом`

#### Этап 2. Подготовить список на очистку

- Показать список содержимого `master` в удобном виде.
- По каждому блоку подготовить статус:
  - оставить
  - удалить
  - обсудить отдельно
- Не удалять ничего до подтверждения по списку.

Результат этапа:
- согласованный список на чистку

#### Этап 3. Очистить `master` после ревизии

- Убрать лишние файлы, мертвый код и временные решения.
- Убрать дубли и спутанную логику, если она не нужна в минимальном варианте.
- Оставить только основу, на которую можно безопасно опираться дальше.

Результат этапа:
- `master` становится заметно проще и понятнее

#### Этап 4. Сформировать нормальную иерархию с учетом `PlatformIO`

При перестройке обязательно учитывать базовую структуру проекта `PlatformIO`:
- `platformio.ini`
- `src/`
- `lib/`
- `include/`
- `test/`

Отдельно проверить:
- чтобы новая структура не ломала сборку
- чтобы имена окружений `PlatformIO` оставались понятными
- чтобы пути и разбиение кода были удобны для дальнейшей поддержки
- чтобы можно было минимизировать код без потери читаемости

Результат этапа:
- утвержденная рабочая структура `master` внутри логики `PlatformIO`

#### Этап 5. Минимизировать код и привести его к порядку

- Разнести ответственность по файлам и модулям.
- Убрать лишнюю связанность.
- Свести `master` к минимальному понятному ядру.
- В начале каждого рабочего файла добавить короткий русский комментарий:
  - что делает файл
  - за что отвечает
  - его роль в проекте

Результат этапа:
- компактный и читаемый код
- в каждом файле есть понятное русское описание сверху

#### Этап 6. Наметить и начать верхний слой на ПК (`MQTT` / `Node-RED`)

- Не тащить старую верхнеуровневую логику как есть.
- Зафиксировать рабочий MQTT-контур на ПК:
  - какой брокер/порт считаем целевым на текущем этапе (`1883` или `1884`)
  - минимальный набор топиков для `master_com12`
  - формат минимального статусного payload
- Собрать минимальный `Node-RED` flow под текущий `master`:
  - подписка на `status` и `online`
  - отображение `board12` / `board13` как `online / offline`
  - базовая проверка восстановления после обрыва связи
- Провести короткий интеграционный прогон: `master` <-> `MQTT` <-> `Node-RED` на этом ПК.

Результат этапа:
- черновой, но рабочий верхний слой на ПК без лишнего наследования старых решений

### Что считаем хорошим итогом дня

- есть полный список содержимого `master`
- зафиксировано правило: сначала понять, потом чистить
- согласован список `оставить / удалить / обсудить`
- определена новая структура с учетом `PlatformIO`
- намечен и частично запущен чистый верхний слой `MQTT` / `Node-RED` на ПК

### Рабочий порядок

Сегодня фокус смещается с внутренней чистки прошивки на проверку внешнего контура `MQTT` / `Node-RED` на ПК. Прошивку `master` используем как минимальную стабильную основу и не расширяем без отдельного решения.
