## Что не было сделано в плане 2

Дата: 2026-03-26

Этот файл фиксирует только те пункты, которые не были закрыты в рамках `Plan/version_2` и переносятся дальше.
То, что было доделано позже отдельными шагами, сюда не включаем.

### Что считаем закрытым в плане 2

- Минимальный `master_control_com12` был доведён до рабочего транспортного слоя.
- Контур `master <-> MQTT <-> Node-RED <-> dashboard` был собран и подтверждён.
- Runtime, шаблоны и документация были синхронизированы.
- Базовая MQTT-безопасность была доведена до рабочего состояния:
  - `1884`
  - узкие listener-ы
  - credentials
  - раздельные учётки
  - `ACL`

### Что не было сделано в плане 2

1. Не был расширен MQTT-контракт.
Остались не открыты темы:
- `cmd`
- `resp`
- `event`

2. Не было принято отдельное решение по командам внутри `master`.
Пока `master` остаётся минимальным транспортом без новой верхнеуровневой логики.

3. Не был сделан следующий функциональный этап после минимального контура.
На конец `version_2` не был выбран и не был реализован новый рабочий слой поверх уже собранной базы.

4. Не был доведён новый операторский веб-интерфейс как отдельный этап.
Минимальный dashboard работал, но развитие визуала, UX и правил интерфейса переносится в `version_3`.

### Что переносим в version_3

- развитие операторского интерфейса;
- правила разработки веб-интерфейса;
- решение по следующему MQTT-контракту;
- решение по следующему уровню логики `master`, если он вообще нужен.
